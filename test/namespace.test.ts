import { MathUtil } from "../src/math-util";

describe('Namespace', () =>{

      it('should support', () => {
            console.info(MathUtil.PI);
            console.info(MathUtil.sum(2,4,6,8,10));
      });
});